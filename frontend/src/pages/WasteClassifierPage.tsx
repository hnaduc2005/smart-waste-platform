import React from 'react';

// [DUMMY FILE]
// File này được tạo tạm thời để bạn có thể chạy (build/dev) dự án locally mà không bị lỗi báo thiếu file.
// VUI LÒNG KHÔNG COMMIT FILE NÀY lên nhánh của bạn (nếu tính năng này do người khác làm).
// Khi thành viên khác push code có chứa thực tế trang này, bạn chỉ cần ghi đè file này là xong.

export default function WasteClassifierPage() {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h2>Trang Nhận diện Rác thải (Waste Classifier)</h2>
      <p>Đây là file giả lập tạm thời (placeholder) để tránh lỗi Vite Cannot Resolve Import.</p>
    </div>
  );
}
