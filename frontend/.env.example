# Frontend uses relative API paths (/api/v1/...) — no env var needed.
# - Docker Production: Nginx proxies /api/ → http://api-gateway:8080/api/
# - Local Dev:         Vite proxies  /api/ → http://localhost:8080/api/   (see vite.config.js)

